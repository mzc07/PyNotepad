[project]
name = "pynotepad"
version = "0.1.0"
description = "Clon del bloc de notas escrito en Python"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mypy>=2.1.0",
    "pytest>=9.0.3",
    "ruff>=0.15.15",
]
